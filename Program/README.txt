To run the program the user need to install the libraries mentioned in the report
including Anaconda

This project is implementing the mathematical representation of neural network
the resource for the course followed is in the report
The methods in main.py and utilsML.py is implemented by the student.

To run, type in the command:

python3.7 main.py 

**
all the files are required to be in the same directory